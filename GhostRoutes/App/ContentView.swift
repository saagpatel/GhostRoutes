import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Ghost Routes")
            .font(.largeTitle)
            .fontWeight(.bold)
    }
}

#Preview {
    ContentView()
}
